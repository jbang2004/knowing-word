<view class="tab-shell">
  <view wx:for="{{list}}" wx:key="pagePath" class="tab-item {{selected === index ? 'active' : ''}}" data-index="{{index}}" bindtap="switchTab">
    <view class="tab-icon icon-{{item.icon}}"><view></view></view>
    <text>{{item.text}}</text>
  </view>
</view>
